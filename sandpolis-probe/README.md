## `sandpolis-probe`

This layer extends management functionality out to agent-less "probe" devices.
Instead of running an agent directly, probes are accessed via well known
protocols like SSH from proper agents (or a server).
