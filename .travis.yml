sudo: false

matrix:
  include:
    - language: java
      name: Java 13 Linux
      os: linux
      jdk: openjdk13
      script:
        - ./gradlew test --console=plain

    - language: java
      name: Java 13 macOS
      os: osx
      jdk: openjdk13
      script:
        - ./gradlew test --console=plain

    #allow_failures:
    #- language: swift
    #  name: Lockstone iOS
    #  os: osx
    #  osx_image: xcode10.3
    #  script:
    #    - ./gradlew :com.sandpolis.viewer.lockstone:build -PLOCKSTONE=true --console=plain
    #  cache:
    #    cocoapods: true
