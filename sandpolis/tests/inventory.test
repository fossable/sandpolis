#!/usr/bin/env bash

## The inventory subsystem has no noninteractive implementation yet; the CLI
## must say so honestly instead of pretending success.
testJsonReportsUnimplemented() {
	! output=$(sandpolis inventory --json --data .)
	echo "${output}" | jq -e '.status == "unimplemented"'
}
