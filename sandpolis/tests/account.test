#!/usr/bin/env bash

## The account subsystem has no noninteractive implementation yet; the CLI
## must say so honestly instead of pretending success.
testJsonReportsUnimplemented() {
	! output=$(sandpolis account --json --data .)
	echo "${output}" | jq -e '.status == "unimplemented"'
}
