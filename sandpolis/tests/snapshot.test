#!/usr/bin/env bash

## The snapshot subsystem has no noninteractive implementation yet; the CLI
## must say so honestly instead of pretending success.
testJsonReportsUnimplemented() {
	! output=$(sandpolis snapshot --json --data .)
	echo "${output}" | jq -e '.status == "unimplemented"'
}
