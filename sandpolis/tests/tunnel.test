#!/usr/bin/env bash

## The tunnel subsystem has no noninteractive implementation yet; the CLI
## must say so honestly instead of pretending success.
testJsonReportsUnimplemented() {
	! output=$(sandpolis tunnel --json --data .)
	echo "${output}" | jq -e '.status == "unimplemented"'
}
