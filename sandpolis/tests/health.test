#!/usr/bin/env bash

## The health subsystem has no noninteractive implementation yet; the CLI
## must say so honestly instead of pretending success.
testJsonReportsUnimplemented() {
	! output=$(sandpolis health --json --data .)
	echo "${output}" | jq -e '.status == "unimplemented"'
}
