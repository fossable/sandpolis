#!/usr/bin/env bash

## The filesystem subsystem has no noninteractive implementation yet; the CLI
## must say so honestly instead of pretending success.
testJsonReportsUnimplemented() {
	! output=$(sandpolis filesystem --json --data .)
	echo "${output}" | jq -e '.status == "unimplemented"'
}
