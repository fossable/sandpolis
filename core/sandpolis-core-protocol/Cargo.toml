[package]
name = "sandpolis-core-protocol"
version = "0.1.0"
edition = "2021"
build = "src/main/rust/build.rs"

[lib]
name = "core_protocol"
path = "src/main/rust/lib.rs"

[dependencies]
prost = { version = "0.9", features = ["prost-derive"] }

[build-dependencies]
prost-build = { version = "0.9.0" }
