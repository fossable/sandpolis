pub struct NetworkData {
    /// The host's hostname
    pub hostname: String,
    /// The host's fully-qualified domain name
    pub fqdn: String,
    /// The host's DNS servers
    pub dns: String,
}
